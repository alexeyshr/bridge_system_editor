## Change

Made `OUR ROOTS` and `SEQUENCE / OPP ROOTS` independently collapsible inside `ROOTS`.

## Details

- Added separate UI toggles for:
  - `OUR ROOTS`
  - `SEQUENCE / OPP ROOTS`
- Works in both `Matrix` and `List` root views.
- Existing root select/edit/delete actions are preserved.

## Validation

- `npm run typecheck` passed
- `npm run lint` passed
- `npm run test` passed
