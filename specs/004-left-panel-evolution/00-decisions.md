# Left Panel Decisions (Locked)

Date: 2026-03-06

1. Smart views scope: implement built-in + custom smart views in MVP.
2. Export format: use schema-based format with `schemaVersion`.
3. Subtree assignment default: `includeFutureDescendants = true`.
4. Section deletion behavior: move child sections to deleted section parent.
5. UI language: English only for now (localization later).
